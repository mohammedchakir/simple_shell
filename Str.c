#include "main.h"

	

	
	free(err);

	free(count);
_strcat(err, msg2);

	_strcat(err, msg);

	_strcat(err, msg3);

	_strccc(err, "\0");

	write(STDERR_FILENO, err, lenght);

	free(err);

	free(count);
_strcat(err, msg2);

	_strcat(err, msg);

	_strcat(err, msg3);

	_strccc(err, "\0");

	write(STDERR_FILENO, err, lenght);

	free(err);

	free(count);
_strcat(err, msg2);

	_strcat(err, msg);

	_strcat(err, msg3);

	_strccc(err, "\0");

	write(STDERR_FILENO, err, lenght);

	free(err);

	free(count);
_strcat(err, msg2);

	_strcat(err, msg);

	_strcat(err, msg3);

	_strccc(err, "\0");

	write(STDERR_FILENO, err, lenght);

	free(err);

	free(count);
_strcat(err, msg2);

	_strcat(err, msg);

	_strcat(err, msg3);

	_strccc(err, "\0");

	write(STDERR_FILENO, err, lenght);

	free(err);

	free(count);
_strcat(err, msg2);

	_strcat(err, msg);

	_strcat(err, msg3);

	_strccc(err, "\0");

	write(STDERR_FILENO, err, lenght);

	free(err);

	free(count);
_strcat(err, msg2);

	_strcat(err, msg);

	_strcat(err, msg3);

	_strccc(err, "\0");

	write(STDERR_FILENO, err, lenght);

	free(err);

	free(count);




}



